feat(rest): implement RestRepository core, registry, helpers and standardize tests

Summary:
- Implement `internal/repositories/rest` core: types, config, errors, helpers, local implementation and high-level `rest_repository.go` (Create/List/Get/Update/Save/Delete).
- Add registry configuration and helpers for request/response/envelope resolution and validation.
- Add tests covering config, errors, helpers, local behavior and repository flow; standardize tests to use `pkg/http_clients/mocks.MockHttpClient` (gomock).
- Introduce `pkg/http_clients/contracts` (HttpClient outbound port) and generated `pkg/http_clients/mocks` (MockHttpClient).
- Update `pkg/repositories/api.go` to re-export repository APIs and add small facade helpers.
- Add documentation: `context-docs/features/rest-repository.md`.
- Fixes and behavior notes: query-param deduplication, header merge with Content-Type default for body requests, nil-response guard on executeRequest.

Files included:
- internal/repositories/rest/* (types, config, error, helper, local, repository, tests)
- pkg/http_clients/contracts/* (HttpClient contract + test)
- pkg/http_clients/mocks/* (generated MockHttpClient)
- pkg/repositories/api.go, pkg/repositories/api_test.go
- context-docs/features/rest-repository.md

This commit stages all current working-tree changes.
